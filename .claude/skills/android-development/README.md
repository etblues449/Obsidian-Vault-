# android-development

The android-development skill lives on the Claude runtime skills mount, not in this repo.
This README keeps the path as a normal tracked folder — it was previously an orphaned git submodule (a gitlink with no .gitmodules), which broke the Vercel build clone.
