# JARVIS memory backup (Phase 3 atomic write) — device-local, never sync
Claude Memory/Account/jarvis_memory.md.bak
Claude Memory/Account/jarvis_memory.md.tmp-*
Claude Memory/Projects/Smart Home/ha-config/hub/**/secrets.yaml
Claude Memory/Projects/Smart Home/ha-config/hub/secrets.yaml


# Obsidian per-machine UI state — never share between devices
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Credentials and regenerable data — never commit.
# 2026-09-05: a live Telethon session (a full Telegram login) was swept into this
# PUBLIC repo by an obsidian-git auto-commit on 2026-08-23. Revoked and removed.
*.session
*.session-journal
Claude Memory/Projects/Trading Signals/tools/signals*.jsonl

# Claude Code harness telemetry (skill-usage PreToolUse hook)
.claude/logs/

