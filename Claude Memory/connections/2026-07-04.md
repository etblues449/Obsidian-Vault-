# Connections Found — 2026-07-04

# JARVIS Connection Analysis — Elliot's Vault

## Surprising Links

**1. Cost Management notes power two projects at once — but only one knows it.**
The *Studying* workspace ("Cost Management coursework — product lifecycle, target costing, value analysis, lifecycle costing") is the *same* source material feeding *Doc to Learning* (its `_index` lists "Source material: Cost Management notes (product lifecycle, target costing, value analysis, lifecycle costing)"). The Other Workspaces table even annotates Studying as "(feeds Doc-to-Learning app)". So Studying isn't just coursework — it's the content pipeline for a shipped app. That's an explicit link, well-evidenced.

**2. Elliot is simultaneously "financial director" (work) and running a personal-finance content channel.**
*Work Financial Forecasting* says "Elliot acts as financial director" on the Select Lifestyles forecast. *Faceless Finance* is a "CA-credentialed, faceless YouTube channel in personal finance/investing" where "CA credential is the intentional trust differentiator." The same CA credential underpins both — one is applied labour, one is content authority. Neither `_index` cross-references the other. Evidence is solid on each side but the *link* is inferred.

**3. "Select Lifestyles" appears in three separate places.**
- Work Financial Forecasting `_index`: the income forecast workbook.
- Other Workspaces: "Select Website — HTML site for Select Lifestyles (`etblues449/Select-lifestyles-Website-` repo)."
- Select Website workspace itself.
Same real-world employer spans forecasting *and* web work. Directly evidenced by shared name + repo.

**4. Two `etblues449` HTML repos, two projects.**
Faceless Finance `_index` notes "Related repo: etblues449/Faceless-Finance (HTML project)"; Select Website uses "etblues449/Select-lifestyles-Website-". Same GitHub account, same single-file-HTML pattern also seen in *Doc to Learning* ("Single-file HTML architecture, no backend"). A consistent build style across three projects — moderately supported.

## Project Bridges

**Smart Home ↔ Linux Pc On Android (strong).**
Smart Home's JARVIS terminal runs "Claude Code pinned to v2.1.112 in Termux" on the Fold 7; *Linux Pc On Android* is explicitly "Termux / mobile-shell experiments." The Termux hard-won lessons (auto-updater kills the 233 MB glibc binary; `read -s` token paste fails on mobile) in Smart Home's Key Decisions are exactly what the Linux-on-Android workspace exists to capture. These should be one knowledge base.

**Smart Home ↔ Code / Claude Code / skills (strong).**
The archived JARVIS phone stack lives at `Claude Memory/Skills/jarvis/phone/`, and *skills* is "Claude Code skills work." The MCP work in Smart Home (`uvx mcp-server-git`, filesystem MCP scoped to `~/jarvis`) is Claude Code experimentation — squarely the *Code* / *Claude Code* territory. Well-evidenced by the shared skills path and MCP tooling.

**Studying → Doc to Learning → (potential) Faceless Finance (partial).**
Studying feeds Doc-to-Learning (evidenced). Doc-to-Learning outputs "Podcast Scripts, Video Scripts, Interactive Lesson Plans." Faceless Finance needs "1–2 original long-form per week." The *tooling* to convert documents into video scripts already exists in the vault — but nothing in either note connects them. This is a bridge that *could* exist; the evidence for a current link is thin.

**Work Financial Forecasting ↔ JARVIS v3 Expense macro (partial).**
JARVIS v3 lists an "Expense" macro and a "Finance Tracker" dashboard (Smart Home `_index`); Work Financial Forecasting is a heavyweight `.xlsm` reconciliation system. These are different finance domains (personal capture vs. work forecasting) and I see no note tying them — flagging as a *non*-link to avoid a false bridge.

## Missing Connections

**1. Faceless Finance has no production pipeline documented — despite the vault holding one.**
Faceless Finance's Next Actions are all "maintain cadence / keep CA prominent" — no mention of *how* scripts get made. Doc-to-Learning generates video scripts. This gap is only visible when you read both notes; neither cites the other.

**2. Smart Home's JARVIS is model-fragmented.**
JARVIS v3 uses `claude-opus-4-8` (Smart Home `_index`); Doc to Learning pins `claude-sonnet-4` (its `_index`). No shared config or model-selection note links the vault's Claude integrations. Thin as a "problem," but worth noting there's no central place tracking which project uses which model/API.

**3. Shared CLAUDE.md instructions span 5 projects but not the two finance ones consistently.**
MEMORY.md says the shared instruction block covers "Studying, Code, Faceless Financial, Notebook lm, Smart Home." Work Financial Forecasting is *not* on that list, despite being the most rule-heavy project (forecasting rules, exclusion codes, VBA constraints). No evidence its instructions are centralised.

**4. Security hygiene is documented once but not propagated.**
Smart Home rotated a GitHub PAT (2026-06-15) and keeps secrets "device-local (localStorage), never synced." Work Financial Forecasting has its own secret-handling rule ("real-name mapping lives only on the work-controlled device, never in this repo"). Same instinct, two projects, no shared "secrets policy" note. The parallel

---
*Generated automatically by n8n (JARVIS Skill 3: Connection Finder).*
