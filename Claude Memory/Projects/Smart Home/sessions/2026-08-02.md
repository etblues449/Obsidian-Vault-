# Session 2026-08-02 — Live ai_cam evidence (screenshots + boot log)

Continuation of [[2026-08-01]] (PR #71). Evidence: full ESPHome boot log + 5 HA device-page
screenshots from Elliot, 00:06–00:14 local.

## Confirmed working on the live device (fw 2026.7.3, built 2026-07-30 00:29, hash 0x8b9a6799)

- Camera up: sensor configured 800x600 JPEG, stream :8080 / snapshot :8081; the missing
  0x3C in the boot I²C scan is the known scan-vs-EXIO3 race, camera unaffected.
- `Camera Power Down` **OFF** (EXIO3 LOW = powered) ✓ · `Amp Enable` **ON** ✓ ·
  ES8311 SLAVE @16 kHz ✓ · ES7210 initialised ✓ · all speaker/mixer timeouts **500 ms** ✓.
- **Assist satellite WORKS**: activity shows Assist satellite Responding → Speaker Playing
  (00:06:52) via pipeline "Home Assistant Cloud 2". A **"Claude" conversation agent** also
  appears in the activity log (00:06:37).
- Device in **Living Room** area ✓ (so area-based voice targeting covers this device).
- WiFi -43 dBm · uptime healthy · firmware reported Up-to-date.

## Defects found

1. **microWakeWord REGRESSION — "Hey Jarvis" not active.** Both `Wake word` and
   `Wake word 2` selects are **unavailable** on this build. The boot log's config dump
   also shows no `micro_wake_word` section. The index's "wake word COMPLETE 2026-07-29"
   predates this firmware (built 00:29 on 07-30) — the rebuild evidently dropped or broke
   the mWW block. Voice is currently push-button ("Start Voice Assistant") only.
   **Fix path:** pull the *current* `ai_cam.yaml` from ESPHome Builder into the vault
   first (the 07-29 config was never captured — session note is a stub), then re-add
   `micro_wake_word` and flash. Do not reconstruct the yaml from memory.
2. **No automations/scripts/scenes reference the AI Cam device** ("Related" is empty) —
   camera + voice satellite is live but nothing in HA automates off it yet.

## Records corrected

- **Live MAC = 28:84:85:49:83:C8** (device page + boot log agree). The 07-23 handoff
  recorded `28:84:85:49:86:70` for ai_cam — that value belongs to the other CAM board or
  was mis-recorded. Which physical board is which: still Elliot's call.
- Flashed config has a tuning layer the vault never captured: Quality/AGC-Ceiling selects,
  Brightness/Contrast/Saturation/Exposure/ISO numbers, Rotate-180, Auto-Exposure/Gain
  switches, Status LED, Boot/User buttons, EXIO6 output (power LED). XCLK now **10 MHz**
  (vault said 20 MHz), JPEG quality **10** (vault said 12).
- Assist pipeline: Assistant + Assistant 2 both "Home Assistant Cloud 2"; finished-speaking
  detection **Aggressive**.

## FIRST FULL HA DIAGNOSIS RUN ✅ (later this session)

Elliot supplied the token + the HA-Green session handoff + the full target `ai_cam.yaml`
(both preserved: [[2026-08-01-hagreen-handoff]] · [[../hardware/ai_cam]]).
**ha-doctor ran successfully via Nabu Casa** → report: [[../diagnostics/2026-08-02-ha-doctor]].

Headlines (full detail in the report):
- Core **2026.8.0b2 BETA** (OS 18.2.rc1) · RUNNING · config valid · 234 integrations · 4 updates pending
- **520 entities, 156 unavailable (30%)** — the dead-node graveyard: bedroom .171, porch servo .206
  (new node!), cctv .234 (a XIAO), espspeaker, Voice PE, landing_ai_cam_2
- **Only 1 of 4 voice satellites is online** (ai_cam). `assist_satellite.landing_ai_cam_2` exists —
  **the 2nd CAM board was already provisioned as a Landing device** — currently unavailable
- **8 automations total** (the "~19 lounge automations" claim does not match the live registry);
  3 stale since May (bedroom_enter/bedroom_empty/landing_someone_enters — consistent with dead
  presence nodes), 1 never fired (landing_room_empty_light_off)
- **Entity naming settled: the PREFIXED form is live** (`camera.living_room_ai_cam_ai_cam`,
  `media_player.living_room_ai_cam_ai_cam_speaker`, …) — index reference updated
- **Canonical TV entity `media_player.tv_jelly_beans_tv_2` NO LONGER EXISTS.** Live candidates:
  `media_player.jelly_beans_tv_tv_jelly_beans_tv` (idle) and `media_player.jelly_beans_tv_3` (on).
  Decision needed → then fix dashboards
- Areas: Living Room 142 · Kitchen 76 · Bedroom 50 · Landing 19 · **Porch 0** · **"Dinning Room"
  (typo — likely the cause of the "dining room" `no_valid_targets` voice miss)**; 10 actionable
  entities with no area (haribo_room light/switch, soundbar_2, jessa_voice_assistant, home_group,
  5× eshare duplicates)
- People/companion healthy: person.elliot_horton home via jelly_bean_s_phone; both phone batteries
  low (15% / 10%)
- Wake-word regression EXPLAINED by the handoff: full mWW config OOMs the Green's compiler
  (`cc1plus` killed at 1165/1615). Options: A drop-wake-word · B compile off-box · **C N100 (~£140)**
- `/api/error_log` denied via Nabu Casa even with this token (check_config + template worked) —
  re-check on-LAN

## Executed later this session ("lets get this done")

- **PR #71 MERGED to master** (`bd91acb`) — everything above is now permanent; ES7210
  `ref: master` consumption unbroken. Working branch restarted from the new master.
- **Area typo FIXED LIVE**: `Dinning Room` → `Dining Room` via the WebSocket registry API
  (verified in the API response). Area id stays `dinning_room` — harmless, only the name matters to Assist.
- **Canonical TV DECIDED from evidence**: `media_player.jelly_beans_tv_3` (device_class
  `tv`, source_list Bose Soundbar/Fire Stick/PS5/iPlayer/Disney+/Netflix, features 221117,
  Samsung UE50NU7470). The other candidate is the DLNA shell (`dlna_dmr`, no sources).
  Propagated to: dashboard v2-corrected (4 refs), ha-doctor canonical check, index Key Decisions.
- **Native es7210 discovery**: the validated ai_cam config resolves `audio_adc: es7210`
  with NO external source — ESPHome ships it natively now. Off-box compile needs nothing special.
- **Option B runbook written**: [[../hardware/ai_cam-compile-runbook]] — PC compile + OTA
  flash, ~15 min, no USB. Same recipe revives `landing_ai_cam_2`.

## Recommendation given

**Option B now** (compile on the Windows PC — full feature set today, £0; bring the vault's
`esphome/components/es7210/` along or merge PR #71 first so `ref: master` resolves), and put
the **N100 on the buy list** as the structural fix — the Green is margin-zero for compiles.

## Recovery night (later, ~01:00) — satellites revived, 2nd board provisioning

Elliot powered everything on. Second diagnosis snapshot ([[../diagnostics/2026-08-02-ha-doctor-2]]):
unavailable **156 → 130**; espspeaker + "Jessa Voice" back online (both offer fw 2026.7.0→2026.7.3);
Voice PE online per Elliot; TV canonical check now PASSES. Main living-room ai_cam was offline
during the snapshot (all four of its checks unavailable) — power-check when done.

**2nd CAM board being flashed as `ai_cam_outside`** (boot log): MAC `28:84:85:49:86:70` —
so the 07-23 handoff's MAC was THIS board; both boards now identified. Got DHCP **.201**,
signal -71 dB. Three fixes needed before building on it:
1. Config lacks `flash_size: 16MB` — bootloader shows 4MB layout (1.75MB app partitions);
   full camera+audio+mWW build won't fit. Fix and reflash first.
2. No API encryption (`Noise encryption: NO`) — add the `api: encryption: key:` block.
3. DHCP .201 → set `manual_ip`/reservation; delete the old `landing_ai_cam_2` ghost device
   once `ai_cam_outside` is adopted.

Registry note: a 6th eshare duplicate appeared (`media_player.eshare_5726_dlna`).
