# Smart Home / JARVIS — Session 2026-08-23

## Summary
Security-hardening + North-Star roadmap session. Locked the app decision, secured the
Vercel companion API, produced the North-Star strategy report, completed Phase 0
(self-knowledge), and built + tested Phase 1's hardline blocklist (not yet shipped).

## Done
- **Locked decision:** six-tab `jarvis-app.mjs` on :8737 is THE daily app. v2 (reactor
  orb, :1875) rejected by user and shelved — boot script archived, process stopped,
  code dormant at `jarvis-core/jarvis2/`. Improvements happen on the six-tab app.
- **Vercel Carousel API secured (verified live):** fail-closed bearer gate on
  `/api/chat` + `/api/capture`; probes no-token->401, wrong->401, correct->200+pong,
  capture->401. Root cause of earlier "still open": Vercel deploys `main`, work was on
  `master` — merged. Submodule gitlink (android-development) de-submoduled.
- **North-Star strategy report** produced (artifact): "Trustworthy Companion" — durable
  honest memory, non-drifting personality, honest self-knowledge, crash-safe
  propose->approve->commit loop, all in the six-tab app; living-mind + multi-model board
  opt-in later. 7-phase roadmap.
- **Phase 0 COMPLETE (verified on device):** `self-knowledge.mjs` reads the live tool
  registry, emits SELF_KNOWLEDGE.md + self-knowledge.json + `--check` drift gate.
  Caught the honest count: **13 callable tools, not 14** (vault-lib.mjs is a helper).
- **Phase 1 BUILT + TESTED (not shipped):** `lib/hardline.mjs` — refuses catastrophic
  pc_control/device commands EVEN IF CONFIRMED (19 patterns; 25/25 tests: 15 block,
  8 allow, nesting, empty-args). Exact `agent.mjs` edit (import + guard block after
  onToolUse, before isSafeMode) shown to user, awaiting go.

## Decisions
- Do not rebuild the injection scanner (`scanForInjection`, 10 patterns) or kill switch
  (`isSafeMode` via env/config/`.jarvis-safe` file) — both already exist and are good.
- Hardline check placed BEFORE safe-mode + confirm, deliberately: no bypass.
- `.jarvis-safe` file is the panic button (creating it refuses all gated actions).

## Blockers / notes
- File-attachment uploads are broken for the user all session — only pasted text +
  screenshots reach Claude. Use those.
- Phone is source of truth; recon before building; ship via vault-base64 + SHA installer.

## Next session starting point
Resume Phase 1: package `lib/hardline.mjs` SHA-verified via vault, one-paste installer
(with agent.mjs.bak backup), user runs it, verify on device that a catastrophic confirmed
command is refused AND a normal one still runs. Then: widen scanForInjection with
exfil patterns; document `.jarvis-safe`. Then Phase 2 (persona-drift fix).
Optional: close the Phase 0 loop (wire capabilitiesBlock into the prompt).
See HANDOFF.md (delivered to user 2026-08-23) for full detail.



## 2026-08-23 UPDATE — Phase 0 loop-closing attempted, regressed (user-reported)
After the session close, work was done (not by the verifying chat) to close the Phase 0
loop (wire capabilitiesBlock into the prompt). Current user-reported device state:
- `self-knowledge.json` shows only **1 tool ("tools")** instead of 13 — i.e. the generator
  that wrote it is NOT the shipped `self-knowledge.mjs` (mine imports allTools() and calls
  it; it produced 13 on device earlier and cannot emit "1 tool called tools"). A static
  grep of lib/tools.mjs can produce that symptom.
- `jarvis.mjs` was patched to inject capabilitiesBlock, but the block is empty/wrong.
- App running clean on :8737.

### The verified facts (use these, do not re-derive by regex)
- **Tools dir scanned:** `~/jarvis-core/tools/` (top-level, NOT lib/tools/). 14 files, 13
  register; `vault-lib.mjs` is a helper (no name/run).
- **The 13 tools:** database, forget, ha_control, ha_list, ha_state, pc_control, remember,
  set_alarm, set_timer, update_memory, vault_list, vault_read, vault_search.

### Correct fix (do NOT hardcode the list — that reintroduces documented!=running drift)
1. Re-run the shipped generator: `cd ~/jarvis-core && node self-knowledge.mjs` — it reads
   each tool's real description and writes a correct `capabilitiesBlock` to
   `self-knowledge.json`. If it still shows 1 tool, self-knowledge.mjs was overwritten —
   re-run the SHA-verified installer:
   `Assistant Core/packages/install-phase0.sh` (pkg SHA c49c93b51fe3f76612c8c4ca831b265ffe2957e712648137477dd9cb89ed4172).
2. Have the prompt READ the json, not a hardcoded string:
   `JSON.parse(readFileSync('self-knowledge.json','utf8')).capabilitiesBlock`.
3. **Patch the DAILY app, `jarvis-app.mjs`'s systemPrompt() (:8737)** — not `jarvis.mjs`
   (that's the one-off text REPL). Each of the 4 entry files has its own systemPrompt();
   the daily driver is jarvis-app.mjs.

### Reminder — Phase 1 (hardline blocklist) is still built+tested but NOT shipped
`lib/hardline.mjs` (25/25 in sandbox) still needs packaging + install + on-device verify.
See HANDOFF.md.



## 2026-08-23 LATER — Phase 0 regression FIXED, Phase 1 VERIFIED, jarvis-core PUSHED

All three verified on device (Fold 7, Termux), not merely written.

### Phase 0 regression — fixed at root, no hardcoding
- Cause confirmed: `self-knowledge.mjs` had been overwritten by a static-parse variant that
  emitted **1 tool ("tools")**. Re-ran the SHA-verified installer
  (`Assistant Core/packages/install-phase0.sh`, pkg SHA `c49c93b5…`) — SHA verified, generator
  restored, regenerated from the LIVE registry: **13 tools**, `--check` drift gate **OK**.
- The list was NOT hardcoded at any point in the final state; the block carries each tool's real
  description (e.g. "database: Query the Supabase agent database by voice or text.",
  "forget [asks first]: Delete a fact from long-term memory."), which only the generator can produce.

### Phase 0 loop CLOSED — in the DAILY app, not the REPL
- Patched `jarvis-app.mjs` (:8737), per the session note's instruction — **not** `jarvis.mjs`.
- Added `capabilitiesBlock(fallbackNames)` helper that reads
  `JSON.parse(readFileSync(new URL('./self-knowledge.json', import.meta.url)))` and returns
  `.capabilitiesBlock`; falls back to a one-line tool list only if the JSON is missing/empty.
- `const caps = capabilitiesBlock(toolNames)` computed beside `facts`; `${caps}` injected
  immediately above "Honesty rules (non-negotiable):" in `systemPrompt()`.
- Patch applied by a Node script with **single-occurrence assertions** on every anchor
  (refuses to run twice; aborts if an anchor is absent/ambiguous). Backup: `jarvis-app.mjs.bak`.
- Verified: `node --check` OK · rendered block **13/13 tools** · fallback **not** used ·
  helper/compute/injection all present in source · app restarted **HTTP 200**.

### Phase 1 — was ALREADY on the phone; now PROVEN
- Correction to this file's earlier "built + tested, not shipped": the device already had
  `lib/hardline.mjs` (5997 B, 01:25) and `lib/agent.mjs` wired — import at **line 22**,
  `blocked-hardline` audit at **line 151**, sitting between `onToolUse` (144) and
  `isSafeMode` (162). Correct order, no bypass. Did not reinstall over it (phone = source of truth).
- On-device proof with `confirm` hardcoded to **always return yes**:
  - `rm -rf /` → `BLOCKED — refused as a catastrophic action (recursive forced delete (rm -rf)).
    This was NOT performed and cannot be, even with confirmation.`
  - `Get-Date` → `EXECUTED: Get-Date`; actually executed: `["Get-Date"]`
  - PROOF PASS (exit 0) — blocks despite consent, and is not a blanket lock.
- Harness note: `executeToolCall` takes `arguments` as a **JSON string**, not an object. A first
  proof attempt passed an object and both calls died at arg-parsing before hardline was reached —
  a false FAIL that proved nothing. `JSON.stringify` the args in any future harness.

### Backup gap CLOSED
- `jarvis-core` pushed to `origin/main`: `bb97f5d..2834aad`. Commits `fce4505` + `2834aad`.
- Last recorded push was 2026-08-06 — 17 days of on-device work (hardline, widened scanner,
  Phase 0, prompt wiring) existed only on the Fold until now.

### Carried forward
- Phase 2 (persona-drift fix) is the next roadmap phase.
- Still open, untouched today: rotate the exposed Carousel `JARVIS_API_TOKEN`; archive the 546M
  `~/jarvis` sprawl to one canonical vault; the Tasker capture leg (capture idle since 2026-07-09).

