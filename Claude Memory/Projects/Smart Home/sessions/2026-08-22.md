# Session 2026-08-22 — JARVIS v2: interface rebuilt, start-up automated, full audit

Claude.ai session. Input: Elliot's mission statement — full and frank audit of the whole
JARVIS effort; fix the two named problems (must type into Termux to start the app; UI
"very busy"); research what the best assistants do; deliver the finished thing in one go,
ready for approval, no mid-task input.

## Audit (fresh vault clone, not summaries)

- Confirmed real & working: vault (drift-check clean), jarvis-core all six tiers,
  skill engine (post 08-02 idempotency fix — briefings 08-03/04/05 on master), memory
  path now proven (1 fact in `jarvis_memory.md`), QA harness consistent.
- Named the recurring failure modes plainly: documented≠merged≠running; silent
  failures believed; stubs presented as products; **the UI was never designed — it
  accreted (6 tabs)**; and `jarvis-core` backup discipline lapses between pushes
  (last recorded push **2026-08-06** per CLAUDE.md — 16 days ago at session time).
- Harness audit (Phase 7-5): agents/skills/orchestrator consistent; only standing item
  is the `webapp-reviewer` model decision (already queued).
- **Correction made mid-session:** an earlier draft of these notes claimed jarvis-core's
  last push was 2026-07-22 (a month stale). CLAUDE.md's change history records a push on
  2026-08-06; all three places carrying the stale figure were corrected before close.

## Research

ChatGPT Voice (Nov-2025 unified interface) and Gemini Live (2026 redesigns) both
abandoned the empty full-screen orb for: voice + live transcript on one screen, the
voice presence condensed to a pill/orb, visible mute/keyboard/end controls, barge-in,
captions. That convergence is the design basis.

## Built & verified (offline — 32/32 assertions green)

**`jarvis2/` package** (SHA-256 `8923a59ac88b45f0dc3480c44909440e1293dec9b05ddd1786b708ce2551dab4`),
zero modifications to existing jarvis-core files, full revert = delete jarvis2/ + boot script:

- `server.mjs` — zero-dep launcher on **:1875**: serves the new UI + full PWA,
  reverse-proxies `/api/*` to jarvis-app.mjs on :8737 (streaming passthrough proven
  incremental in tests), and **auto-spawns the core if down** (proven).
- `web/` — the redesign: one screen, live transcript, tool chips ordered above the
  replies they produced, the amber **reactor** as signature/state/talk-button, visible
  type/stop controls, continuous conversation with sign-off + 3-strike quiet timeout,
  one sheet (Memory / Tools / Status) replacing six tabs, PWA install as "JARVIS",
  every failure surfaced in plain English (incl. a watchdog for browsers that claim
  SpeechRecognition but whose service never starts — found in testing).
- `boot/jarvis-boot` (Termux:Boot, wake-lock first) + `~/.shortcuts/JARVIS` fallback +
  SHA-verified `install.sh`.
- Tests: 15 integration (proxy/static/spawn/streaming pacing/503-not-hang) + 17
  rendered Chromium checks at 385×840 and 840×960 with screenshots, zero console errors.

## Decisions

- v2 fronts the existing core over HTTP rather than rewriting jarvis-app.mjs blind —
  the core's newest code exists only on the Fold, so its HTTP surface is the safe
  contract; the UI's protocol layer is tolerant (SSE/NDJSON/JSON/text) and shows
  diagnostics on anything unexpected.
- Browser-TTS toggle defaults OFF (core already speaks on-device; avoids double audio).
- Phone-side always-on wake word **deliberately not shipped**: Samsung audio-focus
  conflict + Doze + online-STT dependency = the banned kind of workaround. £0 answers:
  side-key remap to the installed app now; ESPHome satellites for room wake (plan of
  record, Option B runbook already patched 2026-08-04).

## Blockers / not verified from here

- Live `/api/chat` event shapes unverifiable (private repo; the newest core code lives
  on the Fold) — mitigated by the tolerant parser + visible diagnostics; tested against
  a faithful mock only.
- On-device behaviour: **the open gate is the 5-minute smoke test** (tap icon → speak →
  reply → "thanks" ends it → reboot phone → icon works cold with Termux never opened).

## Next session starts from

1. Result of the on-device smoke test (report §6 of `JARVIS_V2_REPORT_2026-08-22.md`).
2. One-time steps done? (Termux:Boot from F-Droid opened once; battery Unrestricted for
   Termux + Termux:Boot; Chrome → localhost:1875 → Install.)
3. Push the Fold's `jarvis-core` (now incl. `jarvis2/`) to `main` — last recorded push
   2026-08-06; keep the cadence.



---

## Addendum — later session, same day: package re-built & re-delivered

The build above was recorded correctly, but its downloadable artifact (SHA `8923a59a…`)
was unretrievable from that chat. This later session re-read the vault, re-verified the
design research against live sources (ChatGPT Voice Nov-2025 in-chat transcript + 2026
GPT-Live; Gemini Live's 2026 removal of the fullscreen UI for a pill + visible
transcript/mute/keyboard controls), and re-built the package fresh to the same locked
spec — zero jarvis-core files modified, full revert unchanged.

- **New package SHA-256:** `13471b120b80dd33368c670cf808ce8b01a36f3e7f865319dd4f820dd5dc44b9`
  (supersedes `8923a59a…`; `_index.md` corrected in this commit).
- **Re-verified this session: 43/43 green** — 29 integration assertions (static + PWA +
  proxy passthrough + streaming proven incremental + auto-spawn proven exactly-once +
  503-not-hang in 5 ms) and 14 rendered Chromium checks at 385×840 and 840×960 with
  screenshots, zero console errors.
- **Delivered:** `jarvis2-v2.tar.gz` + SHA-baked `install-jarvis2.sh` +
  `JARVIS_V2_REPORT_2026-08-22.md` (full & frank history, research with sources, build,
  install, §6 smoke test).
- **Open gate unchanged:** the on-device 5-minute smoke test + one-time boot steps,
  then push the Fold's `jarvis-core` to `main`.

