# Session 2026-08-01 — Master plan, HA Doctor, camera-issue post-mortem (PR #71)

Branch `claude/smart-home-expansion-diagnostics-k01qva` · draft PR #71 · remote cloud session (hub NOT reachable — all hub-side claims deferred to ha-doctor).

## Shipped

- **[[../MASTER_PLAN]]** — whole-home roadmap: concrete end-goal definition, laws-of-the-build,
  full verified inventory (14 devices/nodes), HA diagnosis, phases 0–5, buy list, success criteria.
- **HA Doctor** (`Assistant Core/ha-diagnostics/ha-doctor.mjs` + README) — zero-dep read-only
  13-section HA audit runnable from Fold 7/Termux or PC. Tested against a mock hub; the test
  caught 3 real bugs (probe timeout counted as "up", scene field mismatch, `[object Object]`
  rendering) — all fixed. Degrades honestly on non-admin tokens (template/error_log/check_config
  are `@require_admin` — verified against HA core source).
- **Dashboard audit** ([[../diagnostics/2026-08-01-dashboard-audit]], all 5 files read):
  - **S1 FIXED:** canonical `jellybean-dashboard-v2-corrected.yaml` had 4 stale
    `media_player.jelly_beans_tv` refs — the `96e459b` "correction" went the wrong direction.
    Now `media_player.tv_jelly_beans_tv_2` everywhere.
  - S1 OPEN: `living_room_ai_cam_*` (dashboards) vs `ai_cam_*` (index) naming — ha-doctor §12
    now checks both forms and reports which is live.
  - `-v2-final` ≡ `-v2-corrected` (comment-only diff); `dashboard/README.md` added marking canonical.
- **Vendor-BSP verification (web audit)** — all 22 pin/expander claims in the corrected hardware
  guide **CONFIRMED** against BSP v2.0.1 + schematic; zero mismatches. Bonus: full CH32V003
  register map (PWM 0x05, ADC 0x06) + EXIO table → battery-ADC and LCD-backlight backlog items
  now have their register interface documented.
- **EXIO3 boot-race fix PROPOSED** (hardware-guide addendum): blocking `on_boot` priority-700
  settle delay. **NOT flashed — needs the 10-reboot verification on-site.**
- **`.mcp.json`** (repo root) — Playwright MCP (`@playwright/mcp`, verified schema) for browser
  access in future Claude Code sessions. Caveat: no Chromium on Termux — run headed on the PC, or
  serve `--port 8931` from the PC and connect the phone via HTTP transport.

## Camera-issue post-mortem (the pasted Google AI Mode transcript)

Root cause confirmed (already solved 2026-07-23, now vendor-verified): `0x106` is a **power**
problem — OV3660 PWDN is CH32V003 **EXIO3**, not an ESP32 GPIO; stock Arduino sketch never
touches the expander, so all three AI-suggested pinouts were irrelevant (and each was also
wrong). Schematic nuance: CAM_PWDN has a 10K pull-down — vendor BSP relies on it (leaves EXIO3
Hi-Z); ESPHome's `ALWAYS_OFF` drive is equivalent.

## Found this session (new defects)

1. **ES7210 `ref: master` consumption broken** — component commits exist only on the PR branch
   (verified). Merging PR #71 fixes it.
2. **Capture drift** — root `Inbox/` holds 5 newer captures the engine can't see
   (it reads `JARVIS/Inbox/` only). Fold into Phase-2 capture router.
3. `Claude Memory 1/` = stale byte-identical partial duplicate (delete candidate — Elliot's call).
   Root `JARVIS/`-mirror + `Scripts/` duplication also mapped.
4. espspeaker config conflicts with the I²S-mutex law (`timeout: never`, `force_master: true`) —
   retest on that hardware before trusting barge-in.

## Evidence-based queue ticks

Workflows merged + GROQ key + 4 skills green: automated skill commits (1/3/4/6) landed on
master today — observable in `git log`.

## Next (Phase 0 of the master plan)

Merge PR #71 → run ha-doctor (ADMIN token) → fix Areas from its report → back up hub config
into the vault.
