# HANDOFF — 2026-08-21 · AI CAM 2 (`.201`) + Frigate add-on

**For:** the next Claude session, cold start.
**Read first:** `CLAUDE.md` session-start list, then this file, then
[[../fixes/2026-08-21-frigate-go2rtc-crash-loop]].

---

## 0 · TL;DR — where things stand

| Thing | State | Confidence |
|---|---|---|
| `.201` camera hardware + firmware | **WORKING** | Verified on hardware |
| `.201` adopted into HA, entities live | **WORKING** | Verified — live entity state |
| EXIO3 boot-race fix (300 ms settle) | **HOLDING at 5 clean boots** | Verified, protocol stopped early by choice |
| **Frigate add-on** | **🔴 CRASH LOOP — go2rtc never starts** | Diagnosed from logs, **not fixed** |
| Home Assistant core | **HEALTHY** | Verified |
| `.199` AI Cam (living room) | **🔴 OFF NETWORK since 2026-08-06** | Untouched this session |
| `frigatestandalone.yml` in vault | **MISSING — exists only on the Green** | Verified |

**The one thing to do next:** get the Frigate add-on out of its crash loop.
Everything else about `.201` is finished and the wiring config is written and waiting.

---

## 1 · Hard constraint on cloud sessions — read this before planning anything

**A Claude Code session running in the Anthropic cloud container cannot reach the house.**
Probed and confirmed this session:

- `curl http://192.168.0.200:8123/` → **times out** (no route)
- `curl http://192.168.0.201/` → **403 from the egress proxy**, not from the board.
  Do not read that 403 as "the board answered" — it did not.
- The proxy's `noProxy` list includes `192.168.0.0/16`, so LAN addresses are attempted
  direct, and there is no route.

**Consequence:** a cloud session can write configs, diagnose from pasted logs, and
commit to the vault. It **cannot** restart add-ons, SSH to the Green, flash a board, or
verify anything on hardware. Do not offer to. Ask Elliot to run the commands, or work
from a session on the PC.

Earlier in this session the user asked to "take control of this PC" — that is not
possible from here, and saying so early saves a lot of wasted turns.

---

## 2 · What was achieved this session

### `.201` / AI CAM 2 — closed out

- **Adopted into Home Assistant.** ESPHome adoption dialog at `.201:6053`, device shows
  as **AI CAM 2**, all entities present (EXIO3 switches, camera tuning entities, camera).
- **Five consecutive clean boots.** Each boot log showed the ESPHome banner → full device
  init → camera init to **JPEG 800x600 @ q10** → **no `ESP_ERR_NOT_SUPPORTED`** → camera
  streaming (7 images in 60 s). Zero errors across all five.
- **Live entity state confirms it is up** (pasted 21:38):

```yaml
camera.ai_cam_2_ai_cam_outside
  state: idle                       # not "unavailable" — the board is answering
  last_changed: 2026-08-21T21:12:53Z
  last_updated: 2026-08-21T21:38:51Z
  entity_picture: /api/camera_proxy/camera.ai_cam_2_ai_cam_outside?token=...
  friendly_name: AI CAM 2 AI Cam Outside
```

### Reboot protocol — stopped at 5/10 by explicit decision

The run sheet ([[../fixes/2026-08-19-run-sheet-post-camera-fix]] step 2) specifies
**10** power cycles. Elliot said *"no 5 is enough"* and stopped there.

**Record it as 5/10, not as passed.** The original fault was *intermittent* — that is
precisely the failure mode a short run cannot clear. This is a live risk, not a closed
item: if `ESP_ERR_NOT_SUPPORTED` ever reappears at boot after Frigate is wired up, it
will look like a Frigate fault and cost a session to untangle. If it recurs, the fix is
`priority: 700` → `620` in the `on_boot` hook, then restart the count.

### Root cause, for the record

`ESP_ERR_NOT_SUPPORTED` on `.201` was **EXIO3 power-gating**, not a bad sensor. The
CH32V003 I²C expander held the camera powered down until a `priority: 700` `on_boot`
hook drove EXIO3 low and **blocked** for 300 ms before `esp32_camera` initialised. The
blocking lambda is the point — an async `delay:` does not work here.

Ruled out and **not** to be revisited: sensor replacement (JPEG engine confirmed present
— `Pixel Format: JPEG` only prints on a successful init), ESPHome downgrade, hardware swap.

---

## 3 · The live blocker — Frigate add-on go2rtc crash loop

Full diagnosis: [[../fixes/2026-08-21-frigate-go2rtc-crash-loop]]. Summary:

**Two misreadings to avoid — both were made and corrected this session:**

1. **"HA is slow booting."** It is not. HA is healthy — it served live entity state at
   21:38 and is routing ingress traffic. Waiting is not a fix and never was.
2. **"The `.201` reboots broke go2rtc."** They did not. go2rtc starts fine against a
   dead source; it binds its ports and marks the stream offline. An unreachable camera
   cannot stop it binding `1984`.

**What is actually happening:** go2rtc dies at startup before binding port `1984`, every
cycle, emitting no output of its own. The add-on watchdog restarts it; s6's finish script
then times out (`sending it a SIGKILL`), which leaves a process that may still hold
`1984`/`8554` — so the next instance cannot bind either. Self-sustaining. Frigate is
gated behind go2rtc in the s6 chain, so Frigate never starts, so its auth service on
`5001` never binds, so nginx returns **502 on everything** — which is every error in the
log Elliot pasted.

Ports `5000`/`5001` in those errors are **inside the add-on container**, not HA.

**Recovery ladder — stop at the first step that works:**

1. **Full stop → verify "Stopped" → start.** Not *restart* — restart may not clear a
   process s6 cannot reap.
2. **Comment out the entire `go2rtc:` block**, stop/start. Comes up ⇒ that block is the
   culprit; re-add streams one at a time. Prime suspect is an unquoted `#` — YAML
   truncates `ffmpeg:http://...:8080#video=h264` at the `#` unless the value is quoted.
3. **Reboot the host.** Clears a zombie holding the ports. If it loops straight after a
   host reboot, another add-on owns `1984`/`8554`.
4. **Reinstall the add-on.** Recordings are safe (share/media, not the container).
   **Copy `frigatestandalone.yml` off the Green first.**

---

## 4 · The Frigate config to add — ready, verified against the ESPHome file

Only after the add-on comes up clean. Both edits go in `frigatestandalone.yml`.

**go2rtc stream** — note the quotes:

```yaml
go2rtc:
  streams:
    ai_cam_outside: "ffmpeg:http://192.168.0.201:8080#video=h264"
```

**Camera**, alongside the existing `ai_cam:` under `cameras:`:

```yaml
  ai_cam_outside:
    ffmpeg:
      inputs:
        - path: rtsp://127.0.0.1:8554/ai_cam_outside
          roles: [detect, record]
    detect:
      width: 800
      height: 600
      fps: 5
    objects:
      track: [person]
    record:
      enabled: true
      retain:
        days: 14
    snapshots:
      enabled: true
```

**Two deliberate changes from the older block in
[[../fixes/2026-08-04-ai-cam-outside-camera-init]]:**

- `rtsp://127.0.0.1:8554/...` instead of `rtsp://192.168.0.200:8554/...`. The old form
  sends Frigate's own traffic out to the LAN IP and back into the same container.
  `127.0.0.1` is what Frigate documents for a go2rtc restream and removes a failure mode.
- The go2rtc URL is **quoted**. Unquoted, YAML eats `#video=h264`.

**Verified against [[../hardware/ai_cam_outside.yaml]] this session:**
`resolution: 800x600` (L372), `jpeg_quality: 12` (L373), `max_framerate: 5 fps` (L374),
`esp32_camera_web_server` → `:8080` stream / `:8081` snapshot (L383+). So
`detect: 800x600 @ 5fps` is correct. **Change the camera resolution and you must change
`detect:` with it**, or detection silently misaligns with the frame.

**One-client constraint:** the board serves exactly one stream client at a time. go2rtc
must be the only consumer of `:8080`. Close any browser tab sitting on
`http://192.168.0.201:8080` before testing.

---

## 5 · Open drift — unresolved, needs ten minutes on the hub

### 5a · A live speaker entity with no source in the winning config

HA has **`media_player.ai_cam_2_ai_cam_outside_speaker`** (Elliot pasted its media
browser tree). But [[../hardware/ai_cam_outside.yaml]] — the file the index declares the
**winner** for this board — has **no `media_player:` and no `speaker:` block**
(grepped 2026-08-21, zero hits). Only [[../hardware/landing_ai_cam_2.yaml]] has them
(`speaker:` L241, `media_player:` L327).

Two readings, opposite actions:

1. **Registry leftover** from earlier firmware → would read `unavailable` → delete it.
2. **The running firmware is not the vault file** → `hardware/ai_cam_outside.yaml` does
   not describe what is on the board, and the index's "winner" decision rests on a false
   premise.

**Discriminating check:** Developer Tools → States →
`media_player.ai_cam_2_ai_cam_outside_speaker`. `unavailable` ⇒ reading 1. Any real
state ⇒ reading 2.

**Do not treat the `ai_cam_2_` entity prefix as evidence either way.** HA freezes a
device's entity IDs at first adoption and keeps them through later ESPHome node renames.
The index's claim that `ai_cam_outside.yaml` "preserves the existing HA entity IDs"
should be re-checked against this, since that file's node name is `ai_cam_outside`.

### 5b · Which binary is actually on the board — still unanswered

Open since 2026-08-19 (index item; run sheet step 5). The running image self-reports
`ESPHome 2026.7.3 compiled 2026-08-02 01:02:49`. The two 2026-08-04 corrections
(`min_version` removal, idempotent `switch.turn_off: camera_pwdn`) are **not proven** to
be in it. Check the Device Builder last-install timestamp for the device. If it predates
2026-08-04, reflash from the vault — and **restart the reboot count**, because that
changes the boot path. 5a and 5b are probably the same question.

### 5c · `frigatestandalone.yml` is not in the vault

It exists only on the Green. This blocked work twice now, and it makes recovery step 4
genuinely risky. Already logged as **P0** in the index (*"back up hub-side config into
the vault"*). Copy it to `ha-config/` at the first opportunity, along with
`automations.yaml`, `bedroom-2.yaml`, scenes/scripts, and the **live** `ai_cam.yaml`
from ESPHome Builder.

---

## 6 · Not touched this session — `.199` AI Cam (living room)

**Off the network since 2026-08-06.** Different fault class from `.201`; deliberately
left alone while `.201` was in flight. Do not conflate the two.

Observed symptom: go2rtc reporting
`Connection to tcp://192.168.0.199:8080 failed: No route to host`.
**`No route to host` = EHOSTUNREACH — the host is unreachable.** A camera that was
merely failing would give *connection refused*. It worked 2026-08-05, so this is a
change, not a long-standing fault.

**Cause not established — do not record one until observed.** Candidates, cheapest first:

1. Board hung → power-cycle it.
2. An `ai_cam` flash during the 2026-08-04 Device Builder session (its build server
   showed **2 active jobs**, one being AI Cam Outside — the second was never
   identified). This would cross the standing *do not reflash ai_cam* constraint. Check
   the last-install timestamp against the known-good **2026-07-30** build.
3. IP collision at `.199` — `ai_cam.yaml` sets `manual_ip` with **no DHCP reservation**,
   so the router can reassign it. Same class as the open `.171` collision.
   Live MAC `28:84:85:49:83:C8`.

---

## 7 · Suggested order for the next session

1. Ask Elliot to run the **recovery ladder** (§3). Get the add-on log after each step.
2. Add-on green ⇒ apply the **Frigate config** (§4), restart, confirm `ai_cam_outside`
   appears in the Frigate UI with a live stream and person detection firing.
3. While on the hub anyway: run the **5a discriminating check** and settle 5b.
4. **Copy `frigatestandalone.yml` into `ha-config/`** (§5c) — do not defer again.
5. Then, and only then, pick up `.199` (§6) as a fresh fault.
6. Optionally finish boots 6–10 on `.201` (§2).

**Do not** start porting voice/SD features onto `.201`
([[../hardware/landing_ai_cam_2.yaml]] is the porting source) until Frigate is green and
5b is answered. One change per flash, on a board whose camera is confirmed working.

---

## 8 · Reference

- go2rtc crash loop: [[../fixes/2026-08-21-frigate-go2rtc-crash-loop]]
- Camera diagnosis history: [[../fixes/2026-08-04-ai-cam-outside-camera-init]]
- Post-fix run sheet: [[../fixes/2026-08-19-run-sheet-post-camera-fix]]
- Device config: [[../hardware/ai_cam_outside.yaml]]
- Porting source, superseded as a device config: [[../hardware/landing_ai_cam_2.yaml]]
- Project index: [[../_index]]
