# Session 2026-09-05 — logger go-live, broker vetting, security incident, research gate

- **PR #60 merged** to `master` (Trade Guard console + `resolve_trades.py` + due diligence).
- **Logger set up on the Fold 7** (Termux): cloned the vault to `~/Obsidian-Vault-`; bare `pip` had a stale `python3.13` shebang after a Termux upgrade → `python -m ensurepip` + `python -m pip install telethon`; first `--login` failed with `struct.error` (api_id typed as a 10-digit number — not a valid api_id); second login succeeded; listening to **GOLD VIP** (`-1002073063994`). No signals arrived during the session. *Fold 7 screenshot timestamps read 2026-07-16 — device-clock discrepancy vs the vault clock; noted, unresolved.*
- **Elliot's new ask:** hosted web app inside JARVIS + cloud sync + notifications + **real autonomous execution** + `pake-cli` native wrapper.
- **Broker vetting:** `xlence.com` → **rejected** (Tradeco/T4Trade rebrand; FCA/AMF/CMVM/CNMV/FSMA warnings; documented profit-reversal and withdrawal complaints; FX Trust Score / WikiFX / Forex Peace Army). Trading 212 → **legitimate** (FCA 609146, FSCS £85k, 5 licences) but **no retail API**; browser automation of it **refused** (ToS / account-ban risk). **Interactive Brokers chosen** for API execution. Elliot's "I am happy to use t4traders" → refused, with the warning-list facts restated.
- **`/deep-research` launched** on the whole stack: IBKR UK entity/FSCS/gold instruments/API choice (TWS vs Client Portal vs Web API); FCA-regulated alternatives with real APIs (OANDA v20, IG, Saxo, Capital.com, cTrader/MT5 brokers); pake-cli maintenance + mobile support vs Tauri 2 / Capacitor / PWA; Vercel + Supabase as an always-on executor vs a persistent worker; UK regulation/tax (CFD vs spread bet, 1:20 gold leverage, Telegram userbot ToS); strategy base rates + signal-bot failure modes; 3-year deprecation ranking. Plan file drafted in plan mode; **build gated on the verdict.**
- **Codebase map (Explore agent):** `JARVIS-Carousel/` is the only web app in the vault (Next 14 App Router, `next-pwa`, fail-closed bearer auth in `app/api/_auth.ts`, `apiFetch` client, no DB/push/bot/broker code). A Supabase project already exists and is used from `~/jarvis-core` via PostgREST-over-fetch (£0 pattern). Trade Guard maths extracted for a TypeScript port (`DOLLARS_PER_LOT_PER_DOLLAR = 100`, floor-to-0.01 sizing, 6 gates, stats with 95% CI). Gap found: the **5% daily loss cap is displayed but never enforced** in the console. Hobby-plan Vercel project ceiling was hit before → any module lives inside `jarvis-carousel`, not a new project.
- **SECURITY INCIDENT:** `tools/jarvis_tg.session` was tracked on **public** `master` — swept in by obsidian-git auto-commit `c0ce5eb` (2026-08-23). Verified live without exposing the key (256-byte auth key present, DC4, 40 cached entities). **Elliot terminated the session in Telegram → Settings → Devices** (confirmed). This session: `git rm` the file, added `*.session`, `*.session-journal`, `tools/signals*.jsonl`, `.claude/logs/` to `.gitignore`, fixed the README clone path + pip instruction, recorded the incident in `_index.md` as a CORRECTION-class entry. **No history rewrite** — force-push is denied by `permissions.deny` and the revocation makes the leaked bytes worthless. Follow-up idea (not done): a CI check that fails the build if any `*.session` is tracked.
- **Open:** Elliot re-logs the logger after this PR merges; deep-research verdict → plan approval → build.

## Continued 2026-09-06

- **Research verdict landed** (106 agents, 21 confirmed / 4 refuted): IBKR + Vercel + Pake **not** future-proof. Elliot: "find me other brokers" + host = **Raspberry Pi**. Second pass (6 agents, 12 brokers): XTB's API discontinued 2025-03-14; MT5-Python is Windows-only (kills every MetaTrader-only broker on a Pi); Plus500 bans automation; Darwinex default-prohibits it; eToro UK has a new early-access API. Ranking for a Pi: OANDA > Capital.com > Pepperstone/cTrader > IG (£5/pt min stake too big for a £500 account) > Saxo (40-min refresh cliff) > StoneX. **Elliot chose OANDA Europe.** Both passes in [[Research — Executor Stack Verdict 2026-09-05]].
- **Phase 1 built:** `tools/executor/` — config (env only; the 32-bit `api_id` check that would have caught tonight's login error), parser port + validation, risk engine (sizing floors and refuses; stats with 95 % CI; six gates; **daily loss cap and drawdown breaker now enforced**), stdlib OANDA v20 client (orders with `stopLossOnFill`/`takeProfitOnFill`, pricing + home conversion, reconnecting LDJSON streams), local + Supabase store, Telegram Bot alerts, Telethon source, the loop with reconcile-on-start, `--login/--list/--check/--dry-run`, systemd unit, README. **73 tests green** (19 end-to-end against a fake broker: fill, every refusal reason, dry-run, order failure, stream close, live gate ± override).
- Vault: [[Executor — Architecture]] written; `_index.md` — status, the July no-auto-execution decision **superseded under stated conditions**, next actions; `.gitignore` covers executor `.env`, `__pycache__`.
- PR #86 now carries security + research + executor (branch mechanics: token lacks `workflow` scope → server-side update-branch, never force). Elliot to merge or ask for a split.
- **Phase 2a+2b built:** `tools/executor/supabase/schema.sql` (RLS on, no anon policies — the dashboard goes through the gated API route, so no Realtime needed); `JARVIS-Carousel/app/{lib/supabaseAdmin.ts, api/trade/route.ts, trade/layout.tsx, trade/page.tsx}` + manifest shortcut. `next build` green (Next 14.2.35, strict TS); smoke test via `next start`: 401 / 503 / 200 as designed. Found and fixed: `package-lock.json` out of sync (`next-pwa` missing → `npm ci` fails; Vercel only survived because it runs `npm install`). next-pwa's `sw.js`/`workbox-*.js` added to the app's `.gitignore`.
- **Phase 2c built:** Web Push end to end — `worker/index.js` (custom SW via next-pwa), `app/api/push` (subscribe/unsubscribe, upsert by endpoint), `app/api/push/send` (`web-push` fan-out, prunes 404/410), `app/lib/pushClient.ts`, push toggle on `/trade`; worker `WebPushNotifier` + `MultiNotifier` (Telegram + push). `web-push@3.6.7` added. Build green; smoke: 401/503 gates hold; sw.js served. 81 Python tests green after fixing one bad assertion of mine (single-letter substring).
- **Next:** Elliot's OANDA practice + Pi + Supabase + Vercel env setup (Phase 3); first `--check`/`--dry-run` output; merge PR #86.
