# 2026-06-15 — Vault recovery, consolidation & OneDrive de-duplication

## Outcome: DONE — vault repaired, recovered, consolidated, and de-duplicated; all unique content preserved on GitHub

### Claudian plugin
- Updated Claudian to **v2.0.24** (was stuck on v2.0.11; BRAT update never took).
- Hardened config: `excludedTags = [sensitive, private, confidential, legal, financial]`, haiku title model, medium thinking budget, full JARVIS system prompt baked in.
- Fixed an id mismatch (downloaded manifest id `realclaudian` vs folder `claudian`) by setting manifest id back to `claudian`. Plugin confirmed loaded.
- **CAVEAT:** next BRAT update (2.0.25+) reintroduces the id mismatch. Permanent fix = remove from BRAT, install from official store as `realclaudian`.

### Git sync repair
- Root cause of "Failed to sync 1 month ago": local working copy was stuck on a stale `claude/adoring-allen-LY2kF` branch and obsidian-git auto-intervals were all 0.
- Moved to clean `master`, re-enabled obsidian-git auto-sync (autoPullOnBoot, 10-min commit/push), fixed auth via `git config --global credential.helper manager`.

### JARVIS skill recovery
- Recovered the complete **25-file JARVIS skill** (`Claude Memory/Skills/jarvis/`) that was stranded in 9 unmerged commits on the stuck branch — SKILL.md, HANDOFF.md, 7 docs, n8n workflow JSONs, templates, scripts.

### Branch consolidation
- `main` and `master` had diverged; merged `main`'s newer Atlas automation + APPROVAL.md sign-off into `master`, kept all JARVIS work. Pushed.
- Repo bloat fix: restored `.gitignore`, removed 534 accidentally-committed `.smart-env` cache files (106 MiB) + workspace.json from tracking.
- Branch audit done (19 provably-merged, 18 with unique work) — **no branches deleted** per user preference.

### OneDrive duplicate vault — audited & retired
- Confirmed the **correct/active vault is `C:\Users\ElliotHorton\Documents\ObsidianVault`** (git-backed, has everything). The OneDrive "Obsidian Vault — primary" is a stale, non-git duplicate containing 3 nested copies of itself + OneDrive conflict files.
- Diffed both: of **1,358 OneDrive entries, only 17 were truly unique**; most were junk (empty `Untitled.base`, `" 1"` conflict copies, a duplicate of the delivery-standard prompt, test captures).
- **Rescued 5 unique text files** into the git vault (verified on `origin/master`, commit `90fba68`):
  - `Claude Memory/Projects/Smart Home/sessions/2026-06-08.md` (RuView CSI session)
  - `Outlook Organizer/MAPPING.md`, `PROPOSAL.md`, `reference/org-structure.md`
  - `Select Cashflow/this is the path for the folders and files.md`

### Security
- **GitHub PAT rotated** — old exposed token (pasted in a chat on 2026-06-08) revoked; new fine-grained token created and stored in Windows Credential Manager. Verified working: obsidian-git auto-pushed successfully.

## Still open (USER ACTIONS — cannot be done remotely)
- [ ] Copy 2 binary work spreadsheets from the OneDrive vault into `Work/Atlas Training Migration/source-docs/` (text tools can't move binaries). Commands provided in chat.
- [ ] Remove "Obsidian Vault — primary" from the Obsidian launcher (⋮ → Remove from list) to retire the duplicate.
- [ ] (Optional) Permanent Claudian fix: remove from BRAT, reinstall from official store as `realclaudian`.
- [ ] (Optional) Prune leftover `JB's Vault/...` clutter from the repo — harmless but untidy.
- [ ] GitHub *connector* (MCP tool) failing every call — reconnect in Settings → Connectors (does not affect git pushing).

## Key learnings
- OneDrive-synced Obsidian vaults spawn nested self-copies + `" 1"` conflict files — keep the vault out of OneDrive; use git only.
- Filesystem MCP writes text only; binaries need a `cp` in Git Bash (or curl) on the user's machine.
- Verify remote state by fetching `origin/master` in a sandbox clone when the GitHub connector is down.
