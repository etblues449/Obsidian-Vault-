# Patterns Detected

*Last updated: 2026-07-04 (automated, JARVIS Skill 6: Pattern Detector)*

## Week ending 2026-07-04

# Patterns Detected

*Generated by JARVIS Pattern Detector — analysis date 2026-07-04*

---

## Capture Timing

The last 7 days (2026-06-28 → 2026-07-04) are notably sparse. Only **one** capture falls in this window:

- `2026-07-04 04:02` — "your note here" (tasker, empty)

The bulk of activity sits in the **prior fortnight** (2026-06-12 → 2026-06-19), which clusters heavily in two bands:
- **Late night** (22:00–00:00 UTC): the "buy milk" and "cuppa" tasks, plus placeholder captures.
- **Early morning** (02:00–05:00 UTC): the URI questions, "work" note, and smart-home light requests.

No captures at all between 2026-06-20 and 2026-07-03 — an eleven-day gap.

## Action Patterns

- **Repeated identical requests**: "Buy milk" was captured **three times** in an 8-minute span (22:52, 22:53, 23:01 on 2026-06-16), then a "cuppa" reminder 53 minutes later. Suggests either failed confirmation feedback or uncertainty that the capture landed.
- **Placeholder/empty captures dominate the tasker source**: at least six "your note here" and two "test" entries. These are configuration/test artefacts, not real intents.
- **Smart-home requests routinely miss their target**: two light requests, both failing on entity matching (living room lights → only `light.lounge_main` / `light.bedroom_main` exist; and an unspecified "turn off lights").

## Decision Patterns

- **URI unresolved across sessions**: "I need the URI" (2026-06-17) and "What's the URI?" (2026-06-19) — the same open question resurfaced two days apart without resolution logged.
- **Low-information captures left unclarified**: "Work" (single word), "Incomplete capture / This is completed", and the unspecified-lights note all flag *clarify-needed* but show no follow-up.

## Behavioural Patterns

- Elliot tends to fire quick, terse captures rather than detailed ones — most bodies are one line or a repeat of the title.
- Retries when uncertain (milk ×3) rather than checking existing captures.
- Testing behaviour (Fold 7, curl-test, Phase 0/1) is interleaved with real use, muddying the signal.

## Insights

- This is the **first report**, so no prior week exists for comparison — the seeded placeholder confirms no automated report has yet run.
- The dominant failure mode is **feedback uncertainty**: duplicate milk requests and repeated URI questions both point to Elliot not receiving clear confirmation that a capture or answer landed.
- Smart-home intent is real but **entity naming is mismatched** — "living room" vs `lounge_main` is a fixable alias problem, not a user error.
- The recent 7-day window is too thin to trend; the empty 2026-07-04 tasker note suggests the capture pipeline is live but idle.

## Next Week Suggestion

1. **Add a smart-home alias**: map "living room lights" → `light.lounge_main` so requests stop failing silently.
2. **Answer the standing question**: resolve and log "the URI" so it stops recurring.
3. **Confirm-on-capture**: surface a brief confirmation after each capture to curb duplicate submissions (the milk ×3 pattern).
4. **Filter test/placeholder noise**: route "your note here", "test", and curl-test captures out of the analysed set so real patterns aren't diluted.

*Note: evidence base is limited — only one capture in the target 7-day window; conclusions lean on the preceding fortnight and should be re-validated once more recent data accrues.*

---

*Maintained by Skill 6 (Pattern Detector). Updated automatically every Monday 8:00 AM by n8n.*
*Last updated: 2026-07-04 (seeded — first automated report lands Monday 2026-07-06)*

---

*No automated reports yet. The Pattern Detector workflow prepends each week's report above this line.*
